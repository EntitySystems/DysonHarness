# Skills location

- All skills **must** live under the repository root [`skills/`](../skills/) directory (e.g. `skills/<skill-name>/SKILL.md`).
- Do not place skills under `.cursor/skills`, `src/`, or other paths unless they are copies/symlinks of the canonical `/skills` entries.
- New skills are added only under `/skills`.
