@namespace Harness.UI.Components.Shell
@using Harness.UI.Demo
@inject DysonVisualDemoMode DemoMode

@if (DemoMode.Enabled)
{
    <div class="visual-demo-banner" role="status">
        <span class="visual-demo-banner__mark">DEMO</span>
        <span class="visual-demo-banner__mark visual-demo-banner__mark--preview">PREVIEW</span>
        <span class="visual-demo-banner__text">
            Scripted repository-migration promo through the real engine — no model API keys.
        </span>
    </div>
}
