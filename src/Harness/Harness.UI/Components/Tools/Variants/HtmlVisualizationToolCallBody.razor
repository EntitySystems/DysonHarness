@namespace Harness.UI.Components.Tools.Variants
@using Harness.UI.Demo

@{
    var parsed = DysonToolCallUi.TryParseHtmlVisualization(ArgumentsJson);
}

<div class="tool-section">
    <div class="tool-section__label">Visualization</div>
    <div class="tool-section__value">@(parsed?.Title ?? "Untitled visualization")</div>
</div>
@if (parsed is not null)
{
    <div class="tool-section">
        <div class="tool-section__label">Asset sources</div>
        <dl class="tool-details mono">
            <dt>HTML</dt><dd>@parsed.HtmlSource</dd>
            <dt>CSS</dt><dd>@parsed.CssSource</dd>
            <dt>JavaScript</dt><dd>@parsed.JavaScriptSource</dd>
        </dl>
    </div>
}
@if (!ArgsOnly && ResultContent is not null)
{
    <div class="tool-section">
        <div class="tool-section__label">Result</div>
        <pre class="tool-result mono @(IsError ? "tool-result--danger" : null)">@ResultContent</pre>
    </div>
}

@code {
    [Parameter] public string? ArgumentsJson { get; set; }
    [Parameter] public string? ResultContent { get; set; }
    [Parameter] public bool IsError { get; set; }
    [Parameter] public bool ArgsOnly { get; set; }
}
