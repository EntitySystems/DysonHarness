@namespace Harness.UI.Components.Tools.Variants
@using Harness.UI.Demo

@{
    var path = DysonToolCallUi.GetString(ArgumentsJson, "path");
    var offset = DysonToolCallUi.GetInt(ArgumentsJson, "offset");
    var limit = DysonToolCallUi.GetInt(ArgumentsJson, "limit");
}

<div class="tool-section">
    <div class="tool-section__label">File</div>
    <ToolPathLink Path="@path" />
    @if (offset is not null || limit is not null)
    {
        <div class="tool-section__meta mono">
            @if (offset is int o)
            {
                <text>offset @o</text>
            }
            @if (limit is int lim)
            {
                <text>@(offset is null ? null : " · ")limit @lim</text>
            }
        </div>
    }
</div>
@if (!ArgsOnly && ResultContent is not null)
{
    <div class="tool-section">
        <div class="tool-section__label">Content</div>
        <pre class="tool-scroll tool-scroll--tall mono @(IsError ? "tool-scroll--danger" : null)">@ResultContent</pre>
    </div>
}

@code {
    [Parameter] public string? ArgumentsJson { get; set; }
    [Parameter] public string? ResultContent { get; set; }
    [Parameter] public bool IsError { get; set; }
    [Parameter] public bool ArgsOnly { get; set; }
}
